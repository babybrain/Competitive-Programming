#include<bits/stdc++.h>
using namespace std;

#define ll long long

int main(){
    int T;
    cin>>T;
    while(T--)
    {
        ll levels=0,sum=0,n,x;
        cin>>n>>x;
        vector<ll> bin_search(2*n)
    }
}